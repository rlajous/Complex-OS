#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {

	/*int senderPid;
	int consumerNumber;
	char buffer[20];

  if(argc > 0) {
  	parseInt(argv[0],&senderPid);
  	parseInt(argv[1],&consumerNumber);
  }
  else return -1;

  createChannel(senderPid);

  printf("%d,%d\n", senderPid, consumerNumber);

  while(1) {
 		 	receive(senderPid, buffer, 8);
 		 	printf("Consumer %d consumed: %s\n", consumerNumber,buffer);
  }*/
  
  return 0;
}
